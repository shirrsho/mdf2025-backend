export enum StorageType {
  VDO_CIPHER = 'vdochiper',
  DGO = 'digitalocean',
}

export enum StorageStatus {
  READY = 'ready',
}

export enum StorageProvider {
  VDO = 'vdo',
}

export enum MediaType {
  IMAGE = 'image',
  VIDEO = 'video',
}
